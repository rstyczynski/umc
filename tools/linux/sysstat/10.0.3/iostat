#!/bin/bash

delay=$1; shift
count=$1; shift
device=$1

count=$(( $count + 1 ))
iostat -p $device -y -d $delay $count | 
sed $sedBUFFER -n -e '/^Device:/,/^$/{ /^Device/d; /^$/d; p; }'  | 
sed $sedBUFFER 's/  */,/g;s/^,//;s/,$//'

