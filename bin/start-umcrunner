#!/bin/bash
# umcrunnerd start up script 

# the script directory
CUR_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

if [ "$UMC_LOG_DIR" = "" ]; then
  echo >&2 "umc log directory in \$UMC_LOG_DIR variable has not been set!"
  exit 1  
fi 

$CUR_DIR/umcrunnerd >>$UMC_LOG_DIR/umcrunner/umcrunner-$HOSTNAME.log 2>&1 &

